{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "chmod +x ./railway/run-worker.sh && sh ./railway/run-worker.sh",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
